package Appointment;

/**
 * A TimeInterval object represents a period of time.
 * The TimeInterval class provides a method to determine
 * if another time interval overlaps with the time interval
 * represented by the current TimeInterval object.
 */
public class TimeInterval {

    /**
     * Returns true if interval overlaps with this TimeInterval
     * @param interval
     * @return true if interval overlaps with this
     */
    public boolean overlapsWith(TimeInterval interval) {
        /* Implementation not shown */
        return false;
    }

    // There may be fields, constructors, and methods that are not shown.
}
